const
  CENTER = [<%= center %>],
  ZOOM = <%- JSON.stringify(zoom) %>,
  BASEMAP = 'mapbox://styles/mapbox/<%= basemap %>-v9',
  PORT = '<%= port %>',
  ACCESS_TOKEN = '<%= accessToken %>',
  SOURCES = <%- JSON.stringify(sources, null, 2) %>;
